"""
  -set
	-add
	-remove
	-discard
	-pop
	-update
	-union
	-intersection
	-difference
	-isdisjoint
	-issubset
	-issuperset
"""
# set {}, set(), unordered, mutable
arr = {1, 2, 3, 2, 4}
arr.add(40)
print(len(arr),arr) 
